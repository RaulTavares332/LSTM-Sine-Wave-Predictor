# LSTM-Sine-Wave-Predictor
A simple LSTM-based neural network that learns to predict sinusoidal values with added noise. The project demonstrates how to generate synthetic time-series data, train an LSTM model using TensorFlow/Keras, and visualize prediction performance with Matplotlib.
